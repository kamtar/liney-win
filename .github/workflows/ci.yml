name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  # Fast gate for the platform-independent logic. The sources still include
  # Windows headers, so keep this job on Windows as well.
  unit-tests:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Configure + build tests
        run: cmake -S tests -B build-tests -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build-tests --config Release
      - name: Run
        run: ctest --test-dir build-tests --build-config Release --output-on-failure

  # Full Windows build (MSVC + Zig + libghostty-vt). This is the integration
  # check that catches compile breaks in the Win32 / Direct2D / ConPTY code,
  # which the unit tests can't reach. Heavier (fetches + builds Ghostty).
  windows-build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, windows-2025]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
      - name: Set up Zig 0.15.2
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2
      - name: Build
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\build.ps1
      - name: Clean-machine load smoke test
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\smoke-test.ps1 -Exe build-ghostty\Liney.exe
      - name: Startup regression budget
        shell: pwsh
        # Hosted runners use a virtualized display adapter; real-client and
        # local gates retain the stricter default 25 ms frame-p95 budget.
        run: powershell -ExecutionPolicy Bypass -File tools\perf-smoke.ps1 -Exe build-ghostty\Liney.exe -MaxFrameP95Milliseconds 35
      - name: Daily-use tabs, output, and CPU stress
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\daily-use-stress.ps1 -Exe build-ghostty\Liney.exe -MaxFrameP95Milliseconds 35 -Output artifacts\daily-use-stress.json
      - name: GPU recovery, WARP, and DPI matrix
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\display-smoke.ps1 -Exe build-ghostty\Liney.exe
      - name: Deterministic visual regression
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\visual-regression.ps1 -Exe build-ghostty\Liney.exe -ScenarioTimeoutMilliseconds 30000
      - name: Suspend and resume display recovery
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\power-resume-smoke.ps1 -Exe build-ghostty\Liney.exe
      - name: Windows compatibility report
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\compatibility-smoke.ps1 -Exe build-ghostty\Liney.exe -Output compatibility-${{ matrix.os }}.json
      - name: UI Automation and keyboard accessibility
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\accessibility-smoke.ps1 -Exe build-ghostty\Liney.exe
      - name: ConPTY and VT lifecycle soak
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\soak-smoke.ps1 -Exe build-ghostty\Liney.exe -Iterations 50
      - name: Large-output, cancellation, and network-path stability soak
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\stability-soak.ps1 -Exe build-ghostty\Liney.exe -DurationSeconds 30
      - name: Layout backup recovery
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\layout-recovery-smoke.ps1 -Exe build-ghostty\Liney.exe
      - name: Tab, pane, resize, and shell-exit stress
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tools\interaction-stress.ps1 -Exe build-ghostty\Liney.exe -Iterations 5 -IterationTimeoutMilliseconds 30000
      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-build-diagnostics-${{ matrix.os }}
          path: |
            build-ghostty/CMakeFiles/CMakeConfigureLog.yaml
            build-ghostty/**/*.log
            artifacts/visual-regression/**
          if-no-files-found: ignore
      - name: Upload compatibility report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compatibility-${{ matrix.os }}
          path: compatibility-${{ matrix.os }}.json
          if-no-files-found: warn
